# TODO

- [ ] Remove price sorting dropdown from `index.html` and `products.html`.
- [ ] Update `js/app.js` cart modal rendering to remove per-item prices and all currency totals.
- [ ] Update `index.html` / `products.html` cart modal HTML to remove/neutralize subtotal/delivery/total currency placeholders.
- [ ] Update `js/cart-quote.js` to render only Item + Qty (no currency totals).
- [ ] Update `js/quote.js` and `js/quote-storage.js` to remove price columns / currency formatting.
- [ ] Run quick syntax check / build-time check
- [ ] Final verification in browser: cart, wishlist, contact form, filters



 